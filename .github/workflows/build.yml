name: Build
on:
  workflow_run:
    workflows: ["Type Check"]
    types:
      - completed
jobs:
  type-check:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:
      - name: Deploy step
        run: echo "🚀"
